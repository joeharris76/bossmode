[project]
name = "continual-agent-mvp"
version = "0.1.0"
description = "A Codex-native spike for durable task state, evaluation, and gated workflow learning."
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[project.scripts]
continual-agent = "continual_agent.cli:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/continual_agent"]

[tool.pytest.ini_options]
addopts = "-q"
testpaths = ["tests"]

[tool.ruff]
line-length = 100
target-version = "py312"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "SIM"]

[dependency-groups]
dev = [
    "pytest>=9.1.1",
    "ruff>=0.16.4",
]
